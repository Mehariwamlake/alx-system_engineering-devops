#include <stdio.h>
#include <stdbool.h>
int main()
    {
       /* int x=50;
        int y;
        y=10;
        printf("hello world\n");
         printf("%s world\n", "hello");
         printf("print x valu %d\n", x);
         printf("the first no is %d, last no %d\n", x, y);

        printf("enter ur radus: \n");
        int rad;
         scanf("%d", &rad);
         printf("ur radus is %d\n", rad);

printf("enter ur name: \n");
        char name[20];    
        scanf("%s", name);
        printf("ur name is %s\n", name);

        int zero = (double).9999;
        printf("fuck u is %lf\n", zero); 

        //int slices = 27;
        //int people = 2;
        char slices;
        char people;

        printf("enter ur slices\n");
        scanf("%d",slices);

        printf("enter ur peple");
        scanf("%d", people);

        double sliceofpeople = (slices / people);
        printf("%lf\n", sliceofpeople);

        int age = 11;
        double money = 3000;
        bool isadult = true;
        if((age >17 && money > 25000) || isadult)
        {
            printf("this is true\n");
int rows = 4;
int const columes = 4;

int studentgread[rows][columes]={
                            {1,2,3,4},
                            {23,43,56,21},
                            {32,4,5,6}
};
for(int i = 0; i < rows; i++)
{
    for (int j = 0; j < columes; j++)
    {
        printf("%d", studentgread[i][j]);
    }
    printf("\n");
}

*/


        return 0;
    }